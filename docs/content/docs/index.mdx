---
title: Katman
description: The fastest end-to-end type-safe RPC framework for TypeScript.
icon: Rocket
---

import { Cards, Card } from 'fumadocs-ui/components/card';

**5.7x faster** than oRPC. **18x faster** than H3. Single package.

```bash
npm install katman
```

## Why Katman?

- **Fastest pipeline** — Compiled middleware chain, unrolled guard runners
- **Single package** — No `@katman/server` + `@katman/client` + `@katman/contract`
- **Standard Schema** — Works with Zod, Valibot, ArkType
- **3 protocols** — JSON, MessagePack binary, devalue rich types
- **Every runtime** — Node.js, Bun, Deno

## Quick Example

```ts
import { katman, KatmanError } from "katman"
import { z } from "zod"

const k = katman({ context: (req) => ({ db: getDB() }) })
const { query, mutation, guard, router } = k

const auth = guard(async (ctx) => {
  const user = await verify(ctx.headers.authorization)
  if (!user) throw new KatmanError("UNAUTHORIZED")
  return { user }
})

const listUsers = query(
  z.object({ limit: z.number().optional() }),
  async ({ input, ctx }) => ctx.db.users.findMany({ take: input.limit }),
)

const createUser = mutation({
  use: [auth],
  input: z.object({ name: z.string() }),
  errors: { CONFLICT: 409 },
  resolve: async ({ input, ctx, fail }) => {
    if (await ctx.db.users.exists(input.name)) fail("CONFLICT")
    return ctx.db.users.create(input)
  },
})

k.serve(router({ users: { list: listUsers, create: createUser } }), {
  port: 3000,
  scalar: true,
  ws: true,
})
```

<Cards>
  <Card title="Getting Started" href="/docs/getting-started" />
  <Card title="Core Concepts" href="/docs/concepts" />
  <Card title="Client" href="/docs/client" />
  <Card title="Plugins" href="/docs/plugins" />
</Cards>
